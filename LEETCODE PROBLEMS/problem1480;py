class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        l=[]
        sum=0
        for i in range(len(nums)):
            sum+=nums[i]
            l.append(sum)
        return l
#Example 1:
#Input: nums = [1,2,3,4]
#Output: [1,3,6,10]